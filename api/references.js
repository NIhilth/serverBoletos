const pessoas = require('./pessoas');
const usuarios = require('./usuarios');
const boletos = require('./boletos');

const references = {
    pessoas, usuarios, boletos
};

module.exports = references;